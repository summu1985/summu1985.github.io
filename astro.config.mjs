import { defineConfig } from 'astro/config';

export default defineConfig({
  // Replace summu1985 before deployment.
  // If the repository is summu1985.github.io, no base is required.
  site: 'https://summu1985.github.io'
});
